# To Run the dev server and connect to the database

Copy the .env.dev to .env and provide the proper authentications and settings
```sh
npm run dev
```

To generate a tenant api key and then put that on TENANT_API_KEY in the .env
```sh
npm run generate:key 
```
